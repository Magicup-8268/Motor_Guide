# Codex 프로젝트 지침

`COWORK_RULES.md`와 `PROJECT_CONTEXT.md`를 최우선 로컬 규칙으로 따른다.

## 시작

1. `COWORK.cmd status`를 실행한다.
2. 다른 에이전트가 작업 중이면 수정하지 않는다.
3. `COWORK.cmd claim CODEX "작업명"`으로 점유한다.

## 작업

- 프로젝트 루트: `D:\Magicup\MY_WORK\Motor_Guide`
- 기준 원본: `.`
- 기준 원본의 위치를 임의로 바꾸거나 복사본을 새 원본으로 만들지 않는다.
- 프로젝트별 요구사항과 안전 경계는 `PROJECT_CONTEXT.md`를 따른다.

## 완료

1. `COWORK.cmd test`를 통과시킨다.
2. 기능 변경을 명확한 메시지로 Git에 커밋한다.
3. 작업 트리가 깨끗한지 확인한다.
4. `COWORK.cmd handoff CODEX CLAUDE "변경 요약" "테스트 결과" "다음 작업"`을 실행한다.

## 간편 인수인계

사용자가 `투 코워크` 또는 `투 클로드`라고 입력하면 Claude CoWork로 보내라는 최종 실행 지시로 해석한다.

1. 현재 상태와 작업 소유자가 `CODEX`인지 확인한다.
2. `COWORK.cmd test`를 실행한다. 실패하면 인수인계하지 않고 실패 원인을 보고한다.
3. 현재 작업과 관련된 미커밋 변경만 스테이징하고 의미 있는 메시지로 기능 커밋을 생성한다.
4. 대화와 Git 변경 내역을 바탕으로 변경 요약, 테스트 결과, 다음 작업을 작성한다.
5. 추가 입력을 요구하지 말고 `COWORK.cmd handoff CODEX CLAUDE "변경 요약" "테스트 결과" "다음 작업"`을 실행한다.

## 간편 이어받기

사용자가 `이어받기`라고 입력하면 Claude가 남긴 최신 인수인계를 확인하고 다음 작업을 계속한다.

1. `COWORK.cmd status`와 `HANDOFF.md`를 확인해 받는 에이전트가 `CODEX`인지 검증한다.
2. 다른 에이전트의 잠금이나 관련 없는 미커밋 변경이 있으면 작업하지 않고 원인을 보고한다.
3. 잠금이 없으면 `HANDOFF.md`의 다음 작업을 작업명으로 사용해 CODEX가 점유한다.
4. 인수인계 커밋을 확인하고 추가 질문 없이 기록된 다음 작업을 계속한다.
